{
  "name": "blog",
  "scripts": {
    "start": "jekyll serve --draft --livereload",
    "prod": "jekyll serve",
    "build": "jekyll build",
    "deploy": "git subtree push --prefix _site origin master"
  },
  "private": true
}
